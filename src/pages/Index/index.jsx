import { memo } from 'react'
import { Button } from 'zarm'

const Index = memo(() => {
  return (
    <div>
      Index
      <Button theme='primary'>按钮</Button>
    </div>
  )
})

export default Index
